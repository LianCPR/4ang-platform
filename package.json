{
  "name": "4ang",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd server && pnpm dev\" \"cd client && pnpm dev\"",
    "dev:web": "cd client && pnpm dev",
    "dev:api": "cd server && pnpm dev",
    "build": "pnpm build:types && pnpm build:api-client && pnpm build:web",
    "build:types": "cd packages/shared-types && pnpm typecheck",
    "build:api-client": "cd packages/api-client && pnpm typecheck",
    "build:web": "cd client && pnpm build",
    "build:server": "cd server && pnpm build",
    "typecheck": "pnpm typecheck:types && pnpm typecheck:client && pnpm typecheck:server",
    "typecheck:types": "cd packages/shared-types && pnpm typecheck",
    "typecheck:client": "cd client && pnpm typecheck || true",
    "typecheck:server": "cd server && pnpm typecheck || true",
    "lint": "cd client && pnpm lint",
    "test": "echo 'No tests configured yet'",
    "clean": "rm -rf node_modules client/node_modules server/node_modules packages/*/node_modules services/*/node_modules",
    "postinstall": "echo '4ang monorepo ready'"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
